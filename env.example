## Local development (Docker Postgres)
# Copy this file to `.env` locally (do not commit `.env`)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/timer?schema=public
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/timer?schema=public

## Neon (production on Vercel)
# - DATABASE_URL should be Neon *pooled* connection string
# - DIRECT_URL should be Neon *direct/non-pooled* connection string (for migrations)
# Both should include sslmode=require
