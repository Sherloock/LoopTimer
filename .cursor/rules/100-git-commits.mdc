---
description: Git commit conventions used in this repo (only when asked to commit).
alwaysApply: false
---

## Git usage (when asked to commit)
Commit message prefixes:
- `fix:` bug fixes
- `feat:` new features
- `perf:` performance improvements
- `docs:` documentation changes
- `style:` formatting changes
- `refactor:` refactoring
- `test:` tests
- `chore:` maintenance

Rules:
- Use lowercase for commit messages.
- Keep the summary line concise.
- Include a description/body for non-obvious changes.
- Reference issue numbers when applicable.
